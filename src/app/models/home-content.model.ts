export interface HomeContent {
    heroImage: string;
    heroTitle: string;
    heroButtonText: string;

    welcomeTitle: string;

    missionTitle: string;
    missionDescription: string;
    missionImage: string;
}
